/** Shared by the public tracker (joins/tracks) and the admin dashboard (reads only) - in-memory only, never touches the database. */
export const LIVE_VISITORS_PRESENCE_CHANNEL = "analytics-live-visitors";
