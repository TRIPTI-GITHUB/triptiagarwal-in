/**
 * Shared measurements for the LIVE guided-corridor museum
 * (app/museum/[slug]/page.tsx and its components). Do not repurpose
 * these for the new room-based layout experiment - see
 * lib/museum/roomConstants.ts for that instead, kept deliberately
 * separate so the two designs can't collide or break each other.
 */
export const WALL_OFFSET = 4;
export const SHEET_SPACING = 4.5;
export const ROOM_HEIGHT = 4;
export const EYE_HEIGHT = 1.6;
export const FRAME_Y = 1.8;
export const FRAME_WIDTH = 3.7;
export const FRAME_HEIGHT = 2.4;
export const WALK_SPEED = 3.5;